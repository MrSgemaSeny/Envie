import React from 'react';

export const ForYouPage: React.FC = () => {
  // This page is completely empty. The wallpaper is rendered by the Layout component.
  // We just return null so nothing blocks the view.
  return null;
};
