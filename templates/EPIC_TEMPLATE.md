# EPIC: {{НАЗВАНИЕ МОДУЛЯ}}
# Копируй этот файл для каждого нового модуля.
# Имя файла: EPIC_{{MODULE}}.md

---

## Контекст

**Модуль:** {{module-name}}  
**Фаза:** Фаза 3.X  
**Зависит от:** {{список модулей или "scaffold"}}  
**Статус:** [ ] не начато / [~] в процессе / [x] готово

---

## Цель эпика

> Одно предложение: что должно работать когда эпик закрыт?

---

## БД — миграция

**Файл:** `V{{N}}__{{module}}.sql`

```sql
-- Таблицы которые нужно создать
-- Индексы
-- Внешние ключи
```

Чеклист:
- [ ] Миграция написана
- [ ] Миграция проверена на чистой БД (flyway migrate с нуля)
- [ ] Нет конфликтов с предыдущими миграциями

---

## Backend

### Entity
- [ ] `{{Module}}Entity.java` — поля, аннотации JPA
- [ ] Связи с другими entity (если есть)

### Repository
- [ ] `{{Module}}Repository.java` — extends JpaRepository
- [ ] Кастомные запросы если нужны (JPQL, не native SQL без причины)

### DTO
- [ ] `{{Module}}Response.java` — что отдаём клиенту
- [ ] `Create{{Module}}Request.java` — что принимаем при создании
- [ ] `Update{{Module}}Request.java` — что принимаем при обновлении

### Service
- [ ] `{{Module}}Service.java`
- [ ] Методы: create, getAll, getById, update, delete
- [ ] Логика не в контроллере, только в сервисе
- [ ] Нет N+1 запросов

### Controller
- [ ] `{{Module}}Controller.java` — @RestController, @RequestMapping("/api/v1/{{module}}")
- [ ] GET / — список (с пагинацией если список может быть большим)
- [ ] GET /{id} — один элемент
- [ ] POST / — создание
- [ ] PUT /{id} — обновление
- [ ] DELETE /{id} — удаление
- [ ] Все методы возвращают ApiResponse<T>

---

## Frontend (FSD)

### entities/{{module}}
- [ ] `types.ts` — TypeScript интерфейсы
- [ ] `api.ts` — axios вызовы к бэку
- [ ] `index.ts` — публичный экспорт

### features/
- [ ] `create{{Module}}/` — форма создания
- [ ] `update{{Module}}/` — форма редактирования (если нужна)
- [ ] `delete{{Module}}/` — кнопка удаления с подтверждением

### widgets/{{Module}}Card (или аналог)
- [ ] Компонент карточки/отображения элемента
- [ ] Пропсы типизированы

### pages/{{Module}}Page
- [ ] Страница подключена к роутеру
- [ ] Данные загружаются через @tanstack/react-query
- [ ] Состояния: загрузка / ошибка / пустой список / данные

---

## Сценарии для ручного тестирования

- [ ] Создать элемент → появился в списке
- [ ] Обновить элемент → изменения отобразились
- [ ] Удалить элемент → исчез из списка
- [ ] Перезагрузить страницу → данные сохранились
- [ ] Пустой список → UI не падает, показывает заглушку

---

## Признак завершения эпика

> Все сценарии выше пройдены вручную. Данные живут в БД и переживают перезапуск бэка.

---

## Заметки

_(сюда — нестандартные решения, причины отклонений от шаблона)_
